# Decisions Log

Architectural decisions, strategy calls, and naming choices made during homelab development.

---

### 2026-07-14 - node_exporter /mnt exclusion bug fixed; SATA cable swap deferred
**Decision:** Fixed node_exporter filesystem metric exclusion bug rather than silencing the alert rule. Root cause: Debian's `prometheus-node-exporter` package ships with default `--collector.filesystem.mount-points-exclude` regex containing `mnt`, rendering all `/mnt/*` mountpoints (hdd-backup-1, hdd-backup-2, ssd-storage, pve/kinmoon-smb) invisible to Prometheus since at least 2026-05-16. Alert `MountpointMissing_hddbackup1` was stuck in active state for 8 days (2026-07-06 → 2026-07-14) because the underlying metric was never exposed. Fix: edited `/etc/default/prometheus-node-exporter` to remove `mnt` from the exclude regex (`^/(dev|proc|run|sys|media|var/lib/docker/.+|var/lib/containers/storage/.+)($|/)`), restarted `prometheus-node-exporter.service`, confirmed all three /mnt mountpoints now report metrics and alert auto-resolved. Separately: deferred physical SATA cable/port swap for hdd-backup-1 indefinitely — no budget currently available. User clarified hdd-backup-1 is active primary storage for Nextcloud (not a redundant copy); Kinmoon NAS receives only nightly rsync copy at 03:00, not a live mirror. Prior dmesg SATA link error (2026-07-06) was one-time, self-resolved same day — not an ongoing hardware fault.
**Why:** The alert rule logic was correct; the detection mechanism was broken. Fixing the root cause (the exclusion config) rather than silencing the rule preserves alert legitimacy. Dashboards tied to /mnt mountpoints have been silently empty since May 16 and will now populate correctly. This is a Debian package default that could reappear on future OS upgrades, worth documenting. hdd-backup-1 remains necessary as the live landing zone for Nextcloud and vzdump before any offsite sync; no consolidation onto hdd-backup-2 is needed.
**Alternatives considered:** Silence or delete the alert rule (rejected — rule intent was correct, only the metric exposure was broken), pursue physical SATA cable swap immediately (rejected — no budget, and the one-time error has already self-resolved), move Nextcloud to hdd-backup-2 (rejected — no technical need, hdd-backup-1 is functioning correctly once metrics are visible again), treat the 8-day silence as acceptable (rejected — silent Alertmanager failures mask real infrastructure issues).

### 2026-07-09 - Email management assigned to Jeanne Alter, not Da Vinci
**Decision:** Jeanne Alter (user-facing PA) owns the Email Management Pipeline (Read + Notify tier). Da Vinci remains sole Obsidian writer and receives extracted facts via its existing Personal Knowledge gateway rather than gaining a new inbound data source of its own. Scope: 4 personal accounts (muzakkir.kholil06@gmail.com, muzakkirkholil97@icloud.com, hyperjhin00@gmail.com, business.najhin@gmail.com). Work accounts (muzakkir.rahimi@f-secure.com, muzakkir.rahimi.work26@gmail.com) and NSFW-labeled account (adamlil1997@gmail.com) excluded. Behavior tier: Read + Notify (not read-only passive extraction, not full inbox ownership with drafting/sending). Categorization: fixed categories + sender whitelist combined (rejected LLM-only as inconsistent, rejected whitelist-only as too narrow). Permanent vs staged: only bills/payments/subscriptions become permanent second-brain entries; everything else staged in n8n Data Table with ~7 day auto-clear. Model routing: qwen3:14b primary, Claude Haiku API fallback (consistent with Jeanne Alter's existing pattern, not stricter local-only). Credentials: Gmail OAuth2 (n8n native) + iCloud IMAP app-specific password, both in n8n credential store — first concrete step of ecosystem-wide credential store migration. Architecture: 4× per-account trigger workflows (3× Gmail OAuth2 native node, 1× IMAP node) → shared Email Classifier sub-workflow → branches to Telegram notify and/or staging store → permanent-category facts to Da Vinci Personal Knowledge gateway. Schedule: 3x/day (times TBD at build). Status: Design Complete, 0/6 rollout steps implemented.
**Why:** Jeanne Alter is the established user-facing PA and data-aggregator; email is an inbound data source appropriate to its interface role. Da Vinci's boundary as "sole writer, not a data source" is clearer if extraction flows through a gateway rather than granting Da Vinci direct email access. Account scope excludes work (F-Secure cybersecurity policy risk) and NSFW content (sensitive-separate principle). Read + Notify gives proactive value without premature automation of drafting/sending. Combination of categorization + whitelist balances consistency and flexibility. Staging for non-permanent facts prevents second-brain bloat. Standard model routing maintains ecosystem consistency. Credentials in n8n store eliminates hardcoded passwords and begins the planned migration.
**Alternatives considered:** Give Da Vinci direct email access (rejected — breaks "Da Vinci is sole writer" boundary), passive extraction-only tier (rejected — too limited, no proactive value), full inbox ownership with drafting/sending (rejected — premature automation without human-in-the-loop), LLM-only categorization (rejected — inconsistent and hard to audit), whitelist-only (rejected — misses one-off important emails from new senders), local-only model policy (rejected — marginal privacy gain vs. ecosystem consistency), hardcoded credentials (rejected — security risk), include work accounts (rejected — legal/policy risk), include NSFW account (rejected — sensitive-separate principle).

### 2026-07-09 - Funnel Agent design principle established; MERLIN/Midas re-sourced from existing infra
**Decision:** Established "Funnel Agent" classification: an agent is a funnel if there is existing infrastructure it sits on top of and translates into a daily digest with a recommended action. Test: "If there's infra that could be deployed and the agent sits on top and translates it, that agent is a funnel." Classification as of July 9, 2026: MERLIN (Uptime Kuma), Midas (Langfuse), Cu Chulainn (Alertmanager, once built) are funnels; Jeanne Alter, Da Vinci, Scathach are not; EMIYA is hybrid ("funnel-plus" — reads infra like a funnel but also executes on approval). Specific re-sourcing decisions: (1) MERLIN's SSL check now sources from Uptime Kuma's native certificate monitoring instead of direct Cloudflare API (resolves July 14 urgency, removes broken token dependency). (2) Midas's Haiku/Claude costs now source from Langfuse Metrics API instead of duplicate `gilgamesh_costs` Data Table; Ollama tracking stays in Data Table since those calls are $0 and outside Langfuse's automatic cost-inference model. (3) Funnel agents must report a recommendation ("what should I do about it"), not just raw status — this is now a required output field for all funnel implementations going forward.
**Why:** Prevents agents from duplicating effort already solved by dedicated tools (Uptime Kuma, Langfuse, Alertmanager), eliminates drift risk between duplicate data sources (MERLIN's hardcoded SSL date vs. Uptime Kuma's real-time cert tracking; Midas's Data Table costs vs. Langfuse's automatic tracking), and keeps agent build time focused on the actual differentiator — aggregation and actionability — rather than re-implementing plumbing that's already deployed. "What happened + what to do" pattern is the value; the underlying check is commodity.
**Alternatives considered:** Treat all agents uniformly with no funnel/non-funnel distinction (rejected — led directly to MERLIN and Midas duplicating Uptime Kuma and Langfuse functionality instead of reading it), build MERLIN/Midas from scratch without leveraging existing infra (rejected — wastes build time and creates maintenance burden), report raw numbers without recommendations (rejected — incomplete implementation of what makes a funnel valuable).

### 2026-07-09 - Guardian renamed to Cu Chulainn; rename propagation tracked
**Decision:** Agent previously documented as "Guardian" was renamed to "Cu Chulainn" (⚡ Lancer-class servant, Scathach's student in FGO canon) on May 16, 2026. Rename has not yet propagated across documentation. Cu Chulainn should replace all "Guardian" references in agents.md, AI-CONTEXT.md agent table, and decisions.md going forward. Propagation marked as same-category action item as the pending Jeanne Alter rename.
**Why:** Maintains consistency with FGO servant naming convention already established across the ecosystem (Da Vinci, Jeanne Alter, MERLIN, Midas, EMIYA, Scathach, Solomon). Guardian was a placeholder; Cu Chulainn reflects actual agent role (security monitoring, 2nd build priority after Scathach) and thematic consistency.
**Alternatives considered:** Keep Guardian as-is (rejected — inconsistent with ecosystem naming), rename to a different servant (rejected — Cu Chulainn was already designated May 16, no reason to change).

### 2026-07-09 - Phase 27 added: Domain Migration & Infrastructure Audit
**Decision:** New roadmap phase: Phase 27 — Domain Migration & Infrastructure Audit. Sub-phases: 27.1 (audit Cloudflare Access policies, Tunnel routes, SSL, NPM configs, DNS hygiene across existing setup) and 27.2 (migrate nine homelab subdomains from najhin-gaming.com to muzakkir.tech — grafana, n8n, vault, passwords/Vaultwarden, cloud/Nextcloud, finance/Firefly III, ntfy, langfuse, home/Pulse Dashboard). Game server subdomains (mc, terraria, enshrouded, panel) remain permanently on najhin-gaming.com. Migration mechanism: Cloudflare Tunnel routing only (low-risk, no NAT/port-forward rework). Status: Planned / Not Started; muzakkir.tech Cloudflare zone setup directed to begin July 1, 2026 — completion status unconfirmed, verification required next infrastructure session.
**Why:** Separates professional/portfolio infrastructure (muzakkir.tech) from permanent game-server hosting (najhin-gaming.com). Cloudflare Tunnel migration is lower-risk than legacy DDNS. Phase structure (audit before migration) ensures no blind spots before routing changes. Domain split reflects clearer intent: muzakkir.tech as career/portfolio domain, najhin-gaming.com as game server permanent home.
**Alternatives considered:** Keep all subdomains on najhin-gaming.com (rejected — doesn't achieve professional/personal separation), migrate game servers too (rejected — no reason to move permanent game infrastructure), skip audit phase (rejected — risks missed security/config issues).

### 2026-07-09 - Scathach build requires LangGraph evaluation pre-implementation
**Decision:** Scathach (Lancer-class servant, career growth/research agent, 1st build priority in agent roster as of May 16, 2026) must have LangGraph evaluation completed before implementation scope is defined. Career-research workflows expected to require autonomous multi-step reasoning loops that n8n handles poorly for this use case. LangGraph fit assessment is a pre-requisite for Scathach's build scope definition, not optional pre-work. Updated sequencing (July 9, 2026): LangGraph evaluation should happen *after* Phase 24.12 (Jeanne Alter's refactor to n8n's native AI Agent node), not standalone — 24.12 is already testing whether n8n's native AI Agent node can handle multi-step autonomous reasoning for Jeanne Alter, and if it can, that same node may cover Scathach's career-research needs too, avoiding a second orchestration framework. Evaluation still required before Scathach implementation, just sequenced behind 24.12 rather than done independently.
**Why:** Current agents (Jeanne Alter, Da Vinci, MERLIN, Midas, EMIYA) fit n