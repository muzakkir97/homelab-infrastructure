# Decisions Log

Architectural decisions, strategy calls, and naming choices made during homelab development.

---

### 2026-09-22 - Fix najhin-gaming.com now rather than wait for the muzakkir.tech migration (Phase 27.2)
**Decision:** Prioritized fixing najhin-gaming.com's DNS hygiene, Access policies, and TLS settings immediately rather than deferring until Phase 27.2 (Domain Migration to muzakkir.tech). muzakkir.tech does not exist as a Cloudflare zone, so Phase 27.2 cannot start regardless. najhin-gaming.com remains production indefinitely in the meantime, and the patterns fixed this session (wildcard DNS consistency, path-scoped bypass applications) are directly reusable once muzakkir.tech's zone is eventually created.
**Why:** Waiting for Phase 27.2 would leave production infrastructure (Vaultwarden, Nextcloud, n8n, etc.) running with known security gaps (overly-broad Bypass policies, stale DNS, weak TLS) indefinitely. The fixes are not wasted if Phase 27.2 eventually proceeds — they establish best-practice patterns that will be copied to the new zone. The blocking condition for Phase 27.2 (muzakkir.tech zone creation) is entirely separate from these operational fixes.
**Alternatives considered:** Defer all fixes to Phase 27.2 and accept the security gaps until the migration starts (rejected — indefinite exposure to known issues is unacceptable; migration timeline is uncertain), Bundle these fixes into Phase 27.2 as a single large change (rejected — creates unnecessary coupling; the fixes are immediately beneficial to current production), Skip these fixes entirely (rejected — Access and DNS audit found concrete misconfigurations affecting live services).

### 2026-09-22 - DNS A-record correction (direct IP update) over delete-and-consolidate-to-wildcard
**Decision:** Fixed 6 stale A-records for najhin-gaming.com (root, wildcard, grafana, home, n8n, panel) by directly updating each record's content to the current WAN IP (202.184.116.231) rather than deleting the 4 explicit records and relying solely on the wildcard. This was Option A; Option B (delete explicit records and consolidate to wildcard) remains a deliberate future candidate once DDNS token stability is proven.
**Why:** Initial plan for Option B was based on the assumption that the wildcard was already tracking the current WAN IP — re-verification during this session caught that it was equally stale (same root cause: DDNS API token had been invalid since 2026-07-04). Option A is the minimal, immediately-correct fix with no structural risk — it ensures all 6 records are current without relying on assumptions about wildcard state. Option B can be revisited later as a cleanup task once the new DDNS token has been stable for a period.
**Alternatives considered:** Delete explicit records and assume wildcard coverage (rejected — wildcard was equally stale; this option requires proving DDNS stability first), Keep stale records and only update DDNS token going forward (rejected — doesn't fix the immediate outage in DNS resolution; clients were hitting dead IPs), Manually update only the most critical records (rejected — all 6 are in active use; partial fixes create inconsistency).

### 2026-09-22 - Delete orphaned Bypass policies entirely rather than unlink-only
**Decision:** Removed Vaultwarden's orphaned Bypass-Everyone policy and related reusable policies by deleting them outright via the Cloudflare API's top-level policy endpoints, rather than just unlinking them from their applications. Verified via reference check that no other application referenced these policies before deletion.
**Why:** Full deletion eliminates clutter and avoids a stale policy being accidentally relinked or causing confusion in future audits. The reference check confirmed they were truly orphaned, so deletion introduced no risk. An unlinked-but-not-deleted policy serves no purpose and creates technical debt.
**Alternatives considered:** Leave them unlinked in place (rejected — creates clutter and maintenance burden for future audits), Recreate them from scratch with correct scoping (rejected — unnecessary; new policies created during this session are already correct and in use).

### 2026-09-22 - Nextcloud bypass narrowing: create new path-scoped apps BEFORE removing broad bypass
**Decision:** When narrowing Nextcloud's Access policy from whole-domain bypass to 4 specific paths (`/remote.php`, `/ocs`, `/status.php`, `/public.php`), created and linked the 4 new path-scoped bypass applications first, then removed the broad parent-domain bypass policy. This ordering prevented any window where WebDAV/OCS/status/public-share traffic had zero bypass coverage.
**Why:** Removing the broad bypass first would have temporarily broken Obsidian WebDAV sync and other native client connections until the new path-scoped apps were created — even a brief outage risks alerting users or creating confusion. Reversing the order (create new, then remove old) ensures continuous coverage. This is a general principle: add new rules before removing old ones in access-control systems.
**Alternatives considered:** Remove the broad policy first, then create the new apps (rejected — risks breaking Obsidian sync and other active integrations during the window), Create and test the new apps offline before applying them (rejected — unnecessary delay; the ordered creation approach is equally safe and faster).

### 2026-09-22 - Purge Kinmoon's `#recycle` backlog immediately rather than wait for scheduled retention
**Decision:** Manually cleared the existing 774G backlog in Kinmoon's UGOS-level `#recycle` folder (`rm -rf "/volume1/proxmox-backups/#recycle"/*`) immediately rather than waiting only for a forward-looking auto-purge policy. This resolved the capacity crisis (96% → 73.76% used) while Proxmox's own `prune-backups` retention (keep-daily=7, keep-weekly=4) remains the primary safety net.
**Why:** The recycle bin was actively growing in real time as the concurrent backup-daily job ran, consuming space that Proxmox had already logically freed. Concurrent cleanup was safe because the job was actively writing to `dump`, not `#recycle`. Waiting would have left only 118G free on the volume at the time, approaching a dangerous threshold. Manual purging was faster and more urgent than a scheduled retention window.
**Alternatives considered:** Wait for the nightly job to finish before touching the recycle bin (rejected — job posed no conflict; cleanup was safe and more urgent given available free space), Go through UGOS's UI recycle/restore flow instead of direct `rm -rf` (rejected — pre-verified via `ls -la` that contents were only stale backup chunks, not individual items needing review), Only set a forward-looking auto-purge policy without touching the existing backlog (rejected — insufficient; the existing 774G was occupying 23% of the volume in real time).

### 2026-09-22 - CyberPower CP1600EPFCLCD UPS (Pure Sine Wave, RM 1,184) for power protection
**Decision:** Selected CyberPower CP1600EPFCLCD as the UPS for Kuromoon/Kinmoon/core networking from within a RM 2,000 hardware budget. The unit features pure sine wave output (confirmed compatible with Active PFC supplies), genuine power delivery, CyberPower's proven NUT (Network UPS Tools) driver support (one of three mainstream brands), and ~3x headroom over the realistic 200-350W combined load. Added to cart, purchase pending.
**Why:** Addresses a critical failure category (dirty power, unclean shutdown) directly implicated in multi-day recovery incidents already experienced this year (Kinmoon RAID rebuild July 31, Sept 21 host freeze). Near-zero ongoing maintenance burden once installed. Pure sine wave output is essential for Active PFC compatibility, whereas the budget-tier APC options (Easy UPS BV1000I-MS, Back-UPS BX1200MI-MS) use stepped/simulated waveforms with uncertain compatibility and very short battery runtime (1-5 minutes at realistic load). APC Back-UPS Pro (BR-series) has excellent specs but was unavailable on Shopee at an acceptable price point.
**Alternatives considered:** APC Easy UPS BV1000I-MS (RM365, stepped waveform, uncertain NUT support, 1-5 min runtime) — rejected due to waveform compatibility risk and insufficient runtime, APC Back-UPS BX1200MI-MS (RM878, same issues) — rejected for same reasons, APC Back-UPS Pro BR-series (genuine pure sine wave) — rejected due to availability and price on Shopee, Defer UPS purchase and use other RM 2,000 budget items (Kinmoon drive upgrade, RX 6700 XT upgrade, 10GbE networking) — rejected because the UPS addresses a known failure mode with near-zero maintenance, whereas other candidates address no current bottleneck.

### 2026-09-22 - SSH key-based authentication + config aliases instead of self-hosted SSH manager
**Decision:** Implemented SSH key-based authentication across all four core hosts (Kuromoon, Kinmoon, VM 400, Pi-hole) with a single ed25519 keypair (`~/.ssh/id_ed25519_homelab`) and login aliases in `~/.ssh/config` on Minimoon (`kuromoon`, `kinmoon`, `vm400`, `pihole`). This eliminates the need to memorize IPs or passwords and provides password-free access from Minimoon to all four hosts.
**Why:** Solves the stated problem (IP/password fatigue) with zero new attack surface and no dependency on Kuromoon being up. A self-hosted SSH manager (e.g., Guacamole) would require additional infrastructure, a listening port, and would itself be unavailable if Kuromoon fails. SSH config aliases are a standard, minimalist solution.
**Alternatives considered:** Deploy a self-hosted SSH bastion/jump box (Guacamole, Teleport, etc.) — rejected due to infrastructure overhead and single point of failure, Use a password manager to reduce memorization burden — rejected as incomplete; doesn't solve the IP address problem, Keep current manual IP/password approach — rejected as unsustainable for ongoing daily use.

### 2026-09-22 - Tailscale over persistent pfSense firewall rules for Minimoon's cross-VLAN access
**Decision:** After confirming that a Tailscale subnet router already existed on pfSense (advertising VLAN10_MGMT and VLAN30_SERVICES, but with a silently expired auth key), decided to use Tailscale for Minimoon's cross-VLAN access instead of opening persistent firewall rules. Also installed Tailscale directly on Kuromoon (Tailscale IP 100.89.254.28) and VM 400 (100.94.179.99) for redundancy. The three temporary VLAN20_MAIN firewall rules added during the Sept 21 incident (Minimoon → Kuromoon GUI/SSH, Minimoon → Kinmoon web/SSH) were deleted once Tailscale confirmed working.
**Why:** Tailscale trusts revocable device identity (tailnet membership) rather than a spoofable source IP. Given that Minimoon is a daily gaming PC with higher attack surface (mods, cracked game launchers, overlays, etc.), identity-based access control is more defensible than IP-based rules. The existing pfSense subnet route can be instantly revoked or toggled at the device level if Minimoon is ever compromised; a permanent firewall rule cannot. Tailscale also provides encryption in transit and a built-in audit log.
**Alternatives considered:** Keep the temporary pfSense firewall rules as permanent (rejected — IP spoofing risk on a gaming PC is unacceptable; identity-based access is more resilient), Use a VPN client (WireGuard/OpenVPN) on Minimoon (rejected — Tailscale already exists and provides both subnet routing and device-level MFA), Assume Minimoon will never be compromised (rejected — gaming PCs are explicitly higher-risk targets; controls must account for this reality).

### 2026-09-22 - Direct Tailscale install on Kuromoon and VM 400 for access redundancy
**Decision:** Installed Tailscale client directly on both Kuromoon and VM 400, in addition to relying on pfSense's advertised subnet route. This provides two independent network paths to these hosts: the pfSense-advertised route (which assumes pfSense is up and reachable) and direct Tailscale device-to-device connections.
**Why:** Avoids pfSense being a