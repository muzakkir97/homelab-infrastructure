# Changelog

### 2026-09-22 — Phase 37 — n8n Webhook Security Review & Alertmanager Telegram Delivery Fix: Unused Workflows Archived, Token Migration to Secrets File
- **n8n webhook security audit completed (follow-up to Cloudflare Phase 27.1 audit):** of 5 webhook-triggered workflows flagged as lacking secret/header validation, comprehensive review revealed 2 had zero execution history ever (Da Vinci — Sync Docs Pipeline, Midas — CFO Report) and were archived rather than secured — an unused public webhook is pure attack surface with no offsetting functional value; Da Vinci — Update Pipeline and Da Vinci — Inbox Watcher confirmed NOT internet-reachable (Execute-Workflow and Schedule triggers, not Webhook), no action needed; Emiya — Service Down Alert archived after confirming Alertmanager's live config (`/etc/alertmanager/alertmanager.yml`, last modified 2026-02-04) never contained `webhook_configs` block referencing n8n
- **Alertmanager critical-alerts Telegram delivery silently broken since at least 2026-09-21:** bot token had gone invalid; Sept 21 host-freeze critical alert only reached Discord, not Telegram; no monitoring existed on the credential itself (repeating pattern: DDNS token invalid 2.5 months, backup-daily scheduler silent 34 days, now Telegram bot); fixed by creating new dedicated Telegram bot via BotFather (separate from Jeanne Alter's bot, guaranteed distinct by construction, clean separation of "chat with agent" vs. "critical infra alerts"), token moved from inline plaintext in `alertmanager.yml` to permissioned secrets file `/etc/alertmanager/secrets/telegram_bot_token` (owner `alertmanager:alertmanager`, mode 600, directory mode 700), config updated to reference via `bot_token_file` (supported Alertmanager 0.27.0+, verified via `amtool check-config`), reloaded via `systemctl reload alertmanager`
- **Alertmanager Telegram delivery end-to-end test confirmed working:** two synthetic critical alerts (`amtool alert add`) both FIRING and RESOLVED messages confirmed received in Telegram (4 total), chat_id 518832696 unchanged; first test initially failed with "chat not found" (400) until user interacted with new bot in Telegram (Telegram prerequisite for bot-initiated messages), Alertmanager retry logic then succeeded automatically
- **n8n active workflow count reduced:** 3 archived this session (Da Vinci — Sync Docs Pipeline, Midas — CFO Report, Emiya — Service Down Alert); previously 12 listed, 9 remain active/published
- **Existing documentation corrected:** AI-CONTEXT.md Emiya section replaced — previously described "Trigger: Webhook from Alertmanager → Telegram/Discord" but live Alertmanager config routes directly via native integrations, not through n8n; either aspirational documentation never implemented or config drift with no record; actual cause of discrepancy unknown
- **Decisions made:** archived unused webhook workflows (zero execution history = pure risk, deletion stronger than validation); archived Emiya Service Down Alert (confirmed no live Alertmanager dependency via direct grep); used separate dedicated bot for Alertmanager rather than reusing Jeanne Alter's bot (clean functional separation, avoids shared-credential failure modes); migrated Telegram token to permissioned secrets file rather than plaintext swap (justified by recurring credential-failure pattern this week)
- **AI-CONTEXT.md updated:** Emiya — Service Down Alert section rewritten (no live Alertmanager dependency), n8n webhook validation findings documented
- **current-state.md updated:** Alertmanager Telegram bot mechanism (dedicated bot, token in secrets file at `/etc/alertmanager/secrets/telegram_bot_token`, version 0.27.0), n8n workflow count and archived list
- **service-catalog.md updated:** Alertmanager critical-alerts Telegram delivery mechanism (dedicated bot, token stored in permissioned secrets file)
- **decisions.md updated:** rationale for archiving unused webhooks, archiving Emiya despite no live dependency, dedicated bot selection, secrets-file migration decision
- **Action items:** determine if warning-alerts and default Alertmanager receivers should also get Telegram delivery or if Discord-only is intentional; investigate why AI-CONTEXT.md documented Alertmanager→n8n webhook that never existed in live config (aspirational vs. config drift); confirm whether "Emiya" agent concept has other planned/existing responsibilities beyond now-archived Service Down Alert

### 2026-09-22 — Phase 27.1 — Cloudflare Infrastructure Audit: Access Policy Remediation, DNS Hygiene Fix, DDNS Token Replacement, TLS Hardening
- **Phase 27.1 substantially completed this session:** full read-only audit of Cloudflare zones, DNS records, Zero Trust Access policies, Tunnel config, and SSL/TLS settings performed; 17 Access applications on najhin-gaming.com catalogued (up from previous documentation of 9, actual count had drifted undocumented); no muzakkir.tech zone exists on the account (`Hyperjhin00@gmail.com`), confirming Phase 27.2 migration cannot start until zone is created at registrar level
- **Vaultwarden (passwords.najhin-gaming.com) critical misconfiguration fixed:** parent app's Bypass/Everyone policy (which silently overrode Email-OTP Allow policy per Cloudflare's evaluation-order rules) deleted via dashboard after three consecutive API failures (`1010: undefined` errors); only Email-OTP Allow policy (hyperjhin00@gmail.com) now active; `/api` and `/identity` sub-app bypass policies correctly untouched for native client access
- **Nextcloud (cloud.najhin-gaming.com) Access scope narrowed from whole-domain to 4 specific paths:** created new path-scoped Bypass apps for `/remote.php` (WebDAV), `/ocs` (API), `/status.php`, `/public.php` (public share); deleted broad parent-domain Bypass/Everyone policy in sequence to avoid zero-bypass window; web UI, settings, admin, file browser now require Email-OTP; Obsidian WebDAV sync functionality preserved
- **Pulse (home.najhin-gaming.com) security gap closed:** new Access application created with Email-OTP policy (muzakkir.kholil06@gmail.com) — previously had zero Cloudflare-layer auth despite Pulse's lack of built-in authentication
- **najhin-gaming.com zone TLS hardened:** Minimum TLS Version raised from 1.0 to 1.2, Always Use HTTPS enabled (was off), SSL mode confirmed Full (no change needed); required OAuth token scope elevation (initial token was read-only across Zone Settings and Access surfaces), resolved by reconnecting OAuth with broader write scope
- **DNS A-record drift corrected:** 6 stale records on najhin-gaming.com (root, wildcard, grafana, home, n8n, panel) stuck on dead WAN IP 202.184.35.79 (pre-July 5 bridge-mode era); all updated to current WAN IP 202.184.116.231 confirmed via DDNS fix; `mc.` and `terraria.` game-server records (unproxied direct-connect) remain stale at 202.184.35.79, flagged as follow-up but out of scope this session
- **CT 207 (network-ddns) root cause diagnosed and permanently fixed:** actual mechanism is Docker container `favonia/cloudflare-ddns` (not ddclient as previously documented), located at `/opt/cloudflare-ddns/docker-compose.yml`; API token had been invalid since 2026-07-04 21:10 UTC (error 9109 in logs); new token issued this session (scoped to Zone:DNS:Edit, najhin-gaming.com only), installed, container restarted, DDNS confirmed working; `DOMAINS` env var confirmed unchanged (`najhin-gaming.com,*.najhin-gaming.com`)
- **Vaultwarden Access policy deletion challenge:** attempted removal of Bypass-Everyone policies failed three separate ways via API (top-level policy DELETE, app-scoped policy DELETE, PUT to re-link Allow-only) with `1010: undefined` errors; root cause traced to OAuth connection missing write scope entirely (same scope gap that blocked TLS settings PATCH with 9109/10000 errors); resolved by completing deletion manually via Cloudflare dashboard and verifying via API read afterward; verified real-world impact via incognito-browser OTP login test
- **Access application inventory change:** 17 total on najhin-gaming.com (was 13 pre-session, previously documented as 9): panel, langfuse, finance, NextCloud, n8n, vault, Vaultwarden, Vaultwarden API, Vaultwarden Identity, ollama webui, Grafana, n8n Webhooks, Nextcloud Web, Pulse (new), Nextcloud WebDAV/remote.php (new), Nextcloud OCS/ocs (new), Nextcloud Status/status.php (new)
- **Decision made not to defer Phase 27.1 fixes pending Phase 27.2 start:** muzakkir.tech zone doesn't exist and Phase 27.2 is blocked; najhin-gaming.com remains production indefinitely; DNS and Access patterns fixed this session (wildcard hygiene, path-scoped bypass) are directly reusable once muzakkir.tech zone is eventually created — proceeding with najhin-gaming.com hardening now avoids deferring security improvements
- **Decision made on DNS correction strategy:** chose direct IP update (Option A) over delete-and-consolidate-to-wildcard (Option B) as minimal immediately-correct fix with no structural risk; Option B remains a deliberate future option once new DDNS token stability is proven over time
- **n8n webhook endpoints flagged for manual verification:** 5 non-Telegram webhook endpoints (`doc-update`, `doc-sync`, `da-vinci`, `midas-report`, Alertmanager routing) require manual inspection in n8n editor to confirm they validate a secret header/token before trusting Cloudflare-bypassed access — could not be confirmed this session due to Claude Code credential-safety constraints
- **AI-CONTEXT.md updated:** Phase 27.1 Access policy inventory corrected (17 apps, full list), muzakkir.tech zone confirmed nonexistent, CT 207 DDNS mechanism corrected to favonia/cloudflare-ddns Docker container, Vaultwarden Bypass policy deletion documented, Nextcloud Access narrowing documented, najhin-gaming.com TLS settings hardened
- **ROADMAP.md updated:** Phase 27.1 marked substantially complete (Access inventory done, Tunnel routes verified, SSL/TLS audited/hardened, DNS fixed); Phase 27.2 remains blocked at registrar level (zone doesn't exist yet); remaining item: n8n webhook manual validation
- **current-state.md updated:** najhin-gaming.com TLS settings (min_tls_version 1.2, always_use_https on), Access applications count and list, CT 207 DDNS corrected to favonia/cloudflare-ddns with new valid token, DNS A-records corrected to 202.184.116.231
- **service-catalog.md updated:** Vaultwarden Access policy corrected (Email-OTP now enforced on parent login), Nextcloud Access narrowed to 4 paths, Pulse Access application added
- **decisions.md updated:** decision not to wait for muzakkir.tech migration, decision to use direct IP correction for DNS, decision to delete orphaned policies outright, OAuth scope elevation path documented
- **Action items:** manually verify 5 n8n webhook endpoints validate secrets; confirm Obsidian WebDAV sync works end-to-end post-narrowing; fix stale DNS on mc.najhin-gaming.com and terraria.najhin-gaming.com; confirm muzakkir.tech zone creation at registrar before Phase 27.2 starts; confirm Vaultwarden admin panel status; consolidate najhin-gaming.com to wildcard-only DNS once DDNS token stability proven; fix truncated Phase 27.2 section in AI-CONTEXT.md (cuts off mid-sentence at "grafana.najhin-gaming.com →")

### 2026-09-22 — Phase 37 — Kinmoon Volume 1 Capacity Crisis & Backup-Daily Silent Gap Resolution: Recycle Bin Purged, 34-Day Scheduler Silence Investigated, UPS Selected
- **Kinmoon Volume 1 capacity resolved (96% → 73.76% used):** root cause confirmed as UGOS-level `#recycle` folder (774G) accumulating pruned/overwritten backup chunks in real time as `backup-daily` runs, double-counting space already logically freed by Proxmox's own `prune-backups` retention (keep-daily=7, keep-weekly=4); this is the same pattern first diagnosed July 23, 2026, which recurred after July 31 pool rebuild recreated recycle bin with no retention limit set; backlog manually purged via `sudo rm -rf "/volume1/proxmox-backups/#recycle"/*` on Kinmoon; Volume 1 dropped from 2.5TB/2.6TB (96%) to 1.92TB/2.6TB (73.76%), 752G available
- **Auto-purge retention policy (7-14 days) NOT yet set on UGOS shared folder `#recycle`** — still outstanding action item to prevent recurrence; cleanup was temporary; folder will silently refill as backup-daily continues to run
- **CRITICAL discovery — `backup-daily` vzdump job silently stopped firing for 34 days (Aug 18 → Sep 21):** `journalctl -u pvescheduler` showed zero job-start attempts in that entire window (not failures — scheduler never invoked it), meaning all 22 VMIDs (201-208, 211, 213, 214, 220-223, 302-305, 400, 306, 307) had **no backup coverage for over one month**; root cause unconfirmed; `pvescheduler` confirmed alive only since Sep 21 20:38 (hard-reboot timestamp), consistent with but not proof of same host instability behind Sept 21 freeze; documented Sept 3 journald silent-logging gap only explains back half of this window (Sep 3-21), leaving Aug 18-Sep 3 (~16 days) as separate previously-undocumented gap
- **Tonight's backup-daily run (Sep 22, 02:00-03:45) completed successfully for all 22 VMIDs** — job confirmed healthy and running again as of this session; VM 223 was longest pole (58 min, CPU-bound tar process, high file count, not a hang); verified via `ps aux` showing active `R`-state process
- **CT 306 (Enshrouded) and CT 307 (Palworld) confirmed present in `backup-daily` VMID list** via `/etc/pve/jobs.cfg` on Kuromoon — closes backup-coverage gap for these two containers previously documented as missing since July 23, 2026; exact addition date unknown/undocumented
- **VM 400 backup size trending upward:** 32G (May) → 69G (July) → 85-90G (August/September) — real growth in VM's actual disk usage, not backup-mechanism artifact; worth continued monitoring
- **CT 220 autostart race condition reconfirmed recurring:** `/var/log/pve/tasks/active` shows `vzstart:220 ... startup for container '220' failed` from Sept 21 post-incident reboot — consistent with documented pattern (recurring 2026-05-16, 07-06, 08-11, 09-21); still unfixed
- **UPS research and procurement:** evaluated CyberPower CP1600EPFCLCD (Pure Sine Wave, RM 1,184), APC Easy UPS BV1000I-MS (RM 365, stepped waveform, uncertain NUT support), and APC Back-UPS BX1200MI-MS (RM 878, stepped waveform); rejected both APC options due to stepped/simulated sine wave incompatibility with Kuromoon's Active PFC power supply (Ryzen 5 5600X + RX 6700 XT) and short battery runtime (1-5 minutes at realistic load); CyberPower chosen for explicit PFC Sinewave design, pure sine wave output confirmed, well-established NUT support (one of three mainstream-supported brands alongside APC and Eaton), 1000W real rating gives ~3x headroom over combined 200-350W load of Kuromoon + Kinmoon + core networking; **added to Shopee cart, not yet purchased**; deferred other RM 2,000 budget candidates (Kinmoon capacity no longer urgent post-cleanup, GPU upgrades, 10GbE networking) in favor of UPS addressing failure mode (dirty power/unclean shutdown) directly implicated in recent multi-day recovery incidents
- **AI-CONTEXT.md updated:** Kinmoon Volume 1 capacity (96% → 73.76%), backup-daily VMID list now includes 306/307, backup-daily job confirmed healthy, `pvescheduler` uptime/restart timestamp, Sept 21 reboot timestamp
- **ROADMAP.md updated:** Phase 37 sub-item (1) Kinmoon capacity "substantially resolved" but not closed (auto-purge policy pending); Phase 37 sub-item (3) CT 220 autostart race reconfirmed recurring; Phase 37 sub-item (4) Sept 3 journald gap untouched; **new item flagged (not yet added as formal sub-item):** 34-day `backup-daily` scheduler silence root cause investigation
- **current-state.md updated:** kinmoon-smb Volume 1 now 73.76% used (752G available), `backup-daily` VMID list confirms 306/307 inclusion, job completion timestamp (Sep 22 02:00-03:45), pvescheduler service uptime (since Sep 21 20:38)
- **decisions.md updated:** purge Kinmoon `#recycle` backlog immediately via `rm -rf` rather than wait for scheduled retention window (Proxmox's own prune-backups already provides safety net, contents verified as stale backup chunks); UPS selection favoring CyberPower CP1600EPFCLCD over APC options due to PFC Sinewave compatibility and NUT mainstream support
- **Action items:** purchase CyberPower UPS (cart item); set Kinmoon `#recycle` auto-purge retention policy (7-14 days) to prevent recurrence; investigate root cause of 34-day `backup-daily` scheduler silence (Aug 18-Sep 21) — determine if single long stall or host cycling in/out of bad state; fix CT 220 autostart race condition; investigate Sept 3 journald silent-logging gap; consider adding lightweight "backup freshness" watchdog (same pattern as host-freeze watchdog) that alerts if no new file lands in `dump` within ~26 hours (current `mailnotification failure` only catches job errors, not jobs silently never firing); monitor VM 400 backup size growth trend (32G → 85-90G over 4 months) for expected vs. anomalous causes

### 2026-09-22 — Phase 37 — Kuromoon Host Stability & Monitoring: External Watchdog Deployed, SSH Key-Based Access Established, Tailscale Subnet Router Renewed
- **Phase 37 sub-item (2) "external/independent alerting for host-level unresponsiveness" — COMPLETE:** deployed Kuromoon host-freeze watchdog script on Pi-hole (`/usr/local/bin/kuromoon-watchdog.sh`) running via cron every 2 minutes; monitors HTTPS response from Proxmox GUI (port 8006) on both LAN IP (192.168.10.5) and Tailscale IP (100.89.254.28) to distinguish genuine host freeze from routing-only issues; alerts via direct Telegram Bot API (not self-hosted ntfy, which runs on Kuromoon itself and would be unavailable during the failure); fires after 3 consecutive failures (~6 minutes), sends recovery message when back online; state tracked in `/var/tmp/kuromoon-watchdog-*`; fully tested end-to-end by stopping/restarting pveproxy and confirming alert behavior
- **SSH key-based authentication deployed across all four core hosts:** generated ed25519 keypair on Minimoon (`~/.ssh/id_ed25519_homelab`), copied public key to authorized_keys on Kuromoon (root), Kinmoon (Muzakkir), VM 400 (muzakkir), and Pi-hole (pi); created `~/.ssh/config` with aliases `kuromoon`, `kinmoon`, `vm400`, `pihole` — all four confirmed working password-free; replaces need to memorize IPs/passwords
- **Kuromoon SSH access clarified:** Linux system login is `root`, not `muzakkir` (latter is only the internal Proxmox node hostname/API identity, not an OS account); confirmed via successful `root@192.168.10.5` connection
- **Kinmoon SSH access debugging:** required enabling UGOS "Personal Folder" feature with an assigned volume location before OpenSSH would authenticate; UGOS blocks SSH login for any account without a real home directory even when password, Admin role, and global SSH toggle are all correct; Personal Folder auto-populated once Storage app's Volume 1 was viewed; fixed with `mkdir -p ~/.ssh && chmod 700 ~/.ssh` after first access
- **Pi-hole SSH access clarified:** correct OS username is `pi` (Raspberry Pi OS default), not `muzakkir`
- **VM 400 SSH access confirmed:** username `muzakkir`, no issues
- **Tailscale subnet router (`pfsense-homelab`) renewed:** auth key had silently expired Aug 28, 2026; renewed via `tailscale up` in pfSense Diagnostics → Command Prompt; **key expiry now permanently disabled** for this node to prevent future recurrence; was already advertising VLAN10_MGMT (192.168.10.0/24) and VLAN30_SERVICES (192.168.30.0/24)
- **Tailscale also installed directly on Kuromoon and VM 400** in addition to relying on pfSense's subnet route, for redundancy independent of pfSense being available; Kuromoon Tailscale IP `100.89.254.28`, VM 400 IP `100.94.179.99`; Kinmoon not given direct install (UGOS appliance OS), relies on pfSense-advertised route, confirmed working
- **Minimoon Tailscale cross-VLAN access via Tailscale instead of broad pfSense firewall rules:** decided against adding permanent VLAN20→VLAN10/VLAN30 rules because Minimoon is a daily gaming PC with larger attack surface (mods, cracked launchers, overlays); Tailscale trusts revocable device identity rather than spoofable IP; the three temporary VLAN20_MAIN rules added during Sept 21 incident were deleted after confirming Tailscale coverage
- **Kinmoon Storage Pool 1 healthy but at critical capacity:** RAID 1 Normal, both drives active; Volume 1 now at 2.5TB/2.6TB used (96%, "Warning" state in UGOS) — worse than 95% previously logged on 2026-09-21; capacity cleanup now more urgent
- **AI-CONTEXT.md updated with corrected SSH login names and new SSH key/config information**
- **ROADMAP.md updated:** Phase 37 sub-item (2) marked COMPLETE; Phase 37 sub-items (1) Kinmoon drive/capacity, (3) CT 220 autostart race, (4) journald silent-logging gap remain outstanding; Phase 24.11/24.12 still Planned, no implementation started despite extensive design research (n8n-claw analysis, local coding model shortlist)
- **current-state.md updated:** Kinmoon Volume 1 capacity, new SSH access map with `~/.ssh/config` aliases, new Tailscale tailnet with 5 devices (pfsense-homelab with permanent key expiry disabled, Kuromoon, VM 400, Minimoon, Android phone), Kuromoon watchdog script running on Pi-hole via cron
- **Local coding LLM research for RX 6700 XT (12GB VRAM) — shortlisted for future EMIYA execution-engine hands capability:** Qwen2.5-Coder-14B-Instruct and DeepSeek-Coder-V2-Lite-16B selected as candidates; Qwen3-Coder-30B-A3B ruled out as too large even quantized (MoE architecture requires all experts resident in VRAM