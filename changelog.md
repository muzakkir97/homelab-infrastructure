# Changelog

### 2026-08-01 — Incident Resolution — Kinmoon Storage Pool 1 Rebuilt from Scratch, Emergency Backup Restored, libata.force Fix Applied & Verified
- **Continuation of 2026-07-24 incident:** rsync emergency backup launched on 2026-07-24 21:05 was found dead on session start (2026-07-30); root cause SIGINT/SIGTERM/SIGHUP signal interrupt despite `nohup`, resolved by relaunching in `tmux` for session-independent resilience
- **Backup copy resumed with exclusions:** multiple bad-sector read stalls (D-state uninterruptible I/O waits) encountered on Hard Drive 2 during rsync; UGOS event log confirmed `critical medium error` (bad sector, READ operation) on specific files; resolved by excluding affected files and restarting copy; final verified state: 1.2TB matching on both Kinmoon (old) and Kuromoon (emergency copy)
- **SSH access failure diagnosed and resolved:** Kinmoon root/muzakkir logins rejected with "Permission denied"; root cause stale/incorrect password (not SSH config or auto-block); fixed via UGOS web UI password reset; discovered correct case-sensitive username is `Muzakkir` (capital M)
- **Proxmox CIFS storage auth cascaded failure:** after Kinmoon password reset, `kinmoon-smb` mount returned `NT_STATUS_LOGON_FAILURE`; resolved via `pvesm set kinmoon-smb --username Muzakkir --password`
- **libata.force=3.0Gbps GRUB fix applied and verified working:** edited both `/boot/EFI/debian/grub.cfg` (live config) and `/boot/EFI/debian/grub.am` (template, regenerates on firmware updates) on 2026-07-31 to force SATA 3.0Gbps link speed; rebooted and confirmed zero `WRITE FPDMA QUEUED` errors in subsequent full data restore operation; GRUB boot parameter verified present in `/proc/cmdline` post-boot
- **UGOS storage_serv firmware bug identified:** `mdadm --monitor` subprocess mishandles `RebuildFinished` event during active resync, throwing `strconv.Atoi: parsing "-": invalid syntax` and triggering kernel-level `md: recover interrupted` at ~5-7 hours into rebuild; reproduced twice on 2026-07-24 and 2026-07-30; mitigated (not fixed) by stopping `storage_serv` daemon during rebuild attempts; worth reporting upstream to UGREEN
- **Four total RAID 1 rebuild/recovery attempts across two sessions:** two failed on 2026-07-24 (01:32:29 and ~07:06, interrupted by storage_serv bug); two more on 2026-07-30/31 (one with storage_serv running — failed ~5 hours, one with storage_serv stopped — failed at unknown point, root cause never fully identified despite ruling out hardware/cron/systemd/HDD hibernation/ATA power management)
- **Decision to abandon incremental troubleshooting and perform full pool rebuild:** after four failures at different points with different mitigations, determined that root cause of final failures (with storage_serv stopped) was not tractable via further diagnosis; opted instead for destroy-and-recreate approach to restore from already-secured emergency backup
- **Storage Pool 1 fully destroyed and recreated on 2026-07-31 01:10 UTC:** deleted old pool (array UUID `c4e2dde2:...`), created fresh RAID 1 (new array UUID `5bb187d0:b14f67a3:9f4d8ab9:16d079f8`), created Volume 1 (ext4, 2.6TB), recreated shared folder `proxmox-backups` with Read/Write for Muzakkir and Recycle Bin (Admin only); both drives (Hard Drive 1 = Seagate IronWolf 3TB, Hard Drive 2 = Seagate ST3000DM008) status Normal/active sync, zero spares
- **Full data restore from emergency backup completed:** rsync from Kuromoon `/mnt/hdd-backup-2/kinmoon-emergency-backup/` to Kinmoon `/mnt/pve/kinmoon-smb/`, verified matching 1.2TB on both sides; `backup-daily` vzdump job re-enabled (was disabled since 2026-07-24 during rebuild attempts)
- **Files/data permanently lost:** July 12, 2026 dated LXC backup files for containers 202, 203, others, plus one CT211 file dated 2026-07-21; unreadable due to bad sectors on old Hard Drive 2 before array was wiped, assessed as low-impact (newer backups exist for same containers), formally accepted as lost
- **Emergency backup copy retained as safety net:** `/mnt/hdd-backup-2/kinmoon-emergency-backup/` (1.2TB) kept on Kuromoon pending longer-term trust period for rebuilt array; pending deletion per future action item
- **Kuromoon Proxmox changes:** `backup-daily` job re-enabled with targets `kinmoon-smb`, schedule 02:00 daily, retention keep-daily=7/keep-weekly=4; confirmed CT 306 (Enshrouded) and CT 307 (Palworld) already in VMID list from prior session; CIFS credential updated with new Kinmoon password
- **Unrelated issue discovered and fixed: Cloudflare Tunnel misconfiguration blocking passwords.najhin-gaming.com (Vaultwarden):** broken CNAME DNS record pointing to root domain instead of tunnel's `.cfargotunnel.com` target caused Cloudflare Error 522; fixed by deleting and recreating route via tunnel's Published Application Routes UI; secondary issue of Service Type saved as HTTPS instead of HTTP caused Error 502 (tls: first record does not look like TLS handshake), corrected to HTTP
- **Vaultwarden container (CT 214) Docker image updated:** stale image incompatible with current Bitwarden extension's login flow (`POST /identity/accounts/prelogin/password` returned 404); updated to `vaultwarden/server:latest` by pulling and recreating via existing compose file at `/opt/vaultwarden/docker-compose.yml`; data volume untouched; external access now fully functional
- **Action items:** delete emergency backup copy once array trusted long-term; report storage_serv RebuildFinished/strconv.Atoi bug to UGREEN; decide on recovery attempt for lost July 12 LXC backups or formally write off; set sane recycle-bin retention policy on proxmox-backups to avoid repeat of 1.3TB #recycle bloat; verify backup-daily first run (2026-08-02 02:00) succeeds against rebuilt array

### 2026-07-24 — Incident Response — Kinmoon Storage Pool 1 RAID 1 Rebuild Failures, Root Cause Identified, Emergency Backup
- Continuation of Hard Drive 1 replacement from 2026-07-23; new 3TB Seagate IronWolf installed in Bay 1, detected healthy by UGOS
- Storage Pool 1 RAID 1 rebuild attempted twice (2026-07-24 01:32:29 and auto-resumed ~07:06), both failed after ~3 hours and ~5+ hours respectively; currently Degraded, rebuild stopped pending root cause resolution
- **Root cause identified via UGOS event log export and web research:** Hard Drive 2 throws deterministic "failed command: WRITE FPDMA QUEUED" (Serious level) error ~18-19 seconds after every boot/reboot; not a drive health issue (SMART data healthy, Reallocated Sector Count 99 vs. threshold 10), but a known UGREEN DXP2800 hardware/firmware incompatibility at SATA 6.0Gbps link speed with some drive models' narrow signal timing tolerance
- **Known issue confirmed via UGREEN DACH community forum (30+ pages, active thread) and independent troubleshooting (rack2reality.com May 2026):** UGREEN support directly identified the cause and published workaround
- **Fix identified (not yet applied):** force SATA link speed down to 3.0Gbps via kernel boot parameter `libata.force=3.0Gbps`, added to `/boot/EFI/debian/grub.cfg` and `/boot/EFI/debian/grub.am` in vmlinuz boot lines; requires SSH access, file backups, reboot, and verification via `/proc/cmdline`; may require rechecking after future UGOS firmware updates
- Discovered silent UGOS auto-update occurred during incident (2026-07-24 01:23:14, upgraded to v1.17.0.0095, self-triggered reboot) immediately preceding first Hard Drive 2 fault
- Reviewed UGOS event log history: WRITE FPDMA QUEUED signature intermittently present since March 2026 on both drives at different times (2026-03-02, 03-12, 05-08 x2, 05-13/14, 07-23, 07-24), and also a 2026-05-14 06:57 "Failed to detect the hard drive in slot ata1" event; suggests Hard Drive 1's original "failure" triggering this whole incident may have been accelerated or caused by this SATA link instability rather than pure media wear
- **Decided to deprioritize RAID rebuild fix pending data safety:** identified that Kinmoon's vzdump backup archive (1.3TB) is the sole copy of all Proxmox VM/container backup history, unlike Nextcloud data which primarily lives on Kuromoon hdd-backup-1/2 — treating backup data safety as higher priority than completing RAID rebuild while Kinmoon is unstable
- **Emergency backup procedure initiated:** began emergency rsync copy of entire `/mnt/pve/kinmoon-smb/` (proxmox-backups CIFS share, ~1.3TB useful backup data) to Kuromoon's `/mnt/hdd-backup-2/kinmoon-emergency-backup/`, started 2026-07-24 21:05 via `nohup rsync`, PID 3211563, output log at `/root/kinmoon-copy.log` on Kuromoon; copy in progress at session end, not yet verified complete
- Disabled backup-daily vzdump job (`enabled 0` in `/etc/pve/jobs.cfg`) at ~01:30 to prevent 02:00 scheduled run from adding write load during RAID rebuild attempts; remains disabled as of session end — must be re-enabled once Kinmoon confirmed stable
- Confirmed Proxmox GUI "Enable" toggle on backup-daily job would not persist; worked around via direct `/etc/pve/jobs.cfg` CLI edit (pmxcfs hot-loads changes without service restart)
- Decision: defer applying libata.force=3.0Gbps GRUB boot parameter fix until after emergency backup completes, to avoid risk of boot config corruption while Kinmoon holds sole copy of backup data
- Decision: skip third RAID rebuild retry after second consecutive failure, once failure pattern was confirmed deterministic (same ~18-19 second post-boot fault both times) rather than random
- Action items: verify emergency rsync copy completes successfully (du -sh source/dest match, check `/root/kinmoon-copy.log` for errors); apply libata.force=3.0Gbps fix via SSH with documented steps (back up grub files first); retry Storage Pool 1 Repair via UGOS once fix applied and verified in `/proc/cmdline`; re-enable backup-daily only after Kinmoon confirmed stable; add CT 306/307 to VMID list in same edit pass; decide long-term fate of emergency backup copy (keep as extra redundancy vs. remove); verify hdd-backup-2 mount state and usage; consider architectural review of backup-daily single point of failure (Kinmoon-only destination)

### 2026-07-23 — Maintenance Session — Alertmanager iowait Alert Investigation, Backup Infrastructure Audit, Critical Kinmoon Hardware Discovery
- Confirmed house internet outage from 2026-07-20 (ONT PON light off, LOS blinking red — ISP-side fiber fault) RESOLVED as of 2026-07-23
- Investigated CRITICAL "CPU usage 100%" alert on CT 205 (Alertmanager, 192.168.30.205:9100), fired 2026-07-20 03:46-03:47 AM; root-caused to Prometheus's CPU-usage alert rule summing all non-idle time including iowait; `top` confirmed 100.0% `%wa` (I/O wait) with 0.0% `us`/`sy` (zero actual compute load)
- Correlated iowait spike to backup-daily vzdump job (runs 02:00-~03:35 nightly, ~93-96 minute runtime) writing to kinmoon-smb CIFS share at 94% capacity, causing lingering I/O pressure past nominal job completion; documented Prometheus CPU queries count iowait as non-idle, recommend checking `%wa` column in `top` before treating CPU alerts as compute issues
- Audited backup-daily vzdump job configuration: discovered CT 306 (Enshrouded) and CT 307 (Palworld) are NOT included in VMID backup list (201,202,203,204,205,206,207,208,211,213,214,220,221,222,223,302,303,304,305,400) — both game servers have zero backup coverage
- Confirmed kinmoon-smb (Proxmox CIFS storage backing up to Kinmoon NAS, 192.168.10.100) is Proxmox's native vzdump destination, not a separate rsync target; previous documentation describing "nightly rsync at 03:00" was inaccurate; actual mechanism is `backup-daily` job writing directly over CIFS
- Investigated kinmoon-smb disk usage warning (93.3-94% reported, 80%+ threshold crossed); measured actual backup data: `dump` folder (vzdump archives) = 1.3TB, `nextcloud-backup` = 31GB, `#recycle` (UGOS NAS-level recycle bin) = 1.3TB; diagnosed space waste to UGOS retaining deleted files in recycle bin that Proxmox's `prune-backups` policy (keep-daily=7, keep-weekly=4) already purged — double-counting backup churn, not unbounded accumulation
- **CRITICAL DISCOVERY — Kinmoon Hard Drive 1 Hardware Failure:** navigated UGOS Control Panel (192.168.10.100:4000) Storage app and confirmed Storage Pool 1 (RAID 1) degraded status:
  - Hard Drive 1 SMART status: **Critical**
  - Reallocated Sector Count (Index 5): present value **133**, below failure threshold **140**, trending 573 (March) → 609 (April) → 1,963 (May) — genuine progressive media degradation
  - Hard Drive 2: healthy, 46°C, no reported faults
  - RAID 1 mirror currently running on **single healthy drive with zero redundancy**
  - Diagnosis: genuine media failure on Hard Drive 1, NOT a cable/interface issue (no UDMA_CRC errors, unlike Kuromoon's hdd-backup-1 SATA cable problem from 2026-07-14)
  - **Current exposure:** if Hard Drive 2 fails before Hard Drive 1 is replaced, entire proxmox-backups archive (2.4TB of vzdump history, only offsite/downstream copy) would be lost; no live production data at risk (hdd-backup-1/hdd-backup-2 on Kuromoon remain healthy primary storage)
  - Volume 1 additionally flagged at <10% available capacity (warning from UGOS)
- Decision: Do NOT click "Repair" on Kinmoon Storage Pool 1 until Hard Drive 1 is physically replaced; Repair triggers a RAID 1 rebuild requiring a healthy second member, would either be a no-op or stress the failing drive further, risking harder failure; correct sequence is hardware replacement first, then Repair/rebuild
- Decision: Defer Kinmoon-smb recycle bin cleanup (1.3TB low-stakes space optimization) in favor of addressing Hard Drive 1 failure (live risk to backup redundancy) first
- Documented kinmoon-smb CIFS share structure: 2.6TB volume (ext4), 2.4TB used, 35,709 files, 8,239 subfolders, created 2026-05-09; actual useful backup data ~1.3TB (real vzdump archives), remainder is overhead + deleted-but-retained files in NAS recycle bin
- Action items: source and budget replacement drive for Kinmoon Hard Drive 1 (RAID 1 member, 2-bay DXP2800 — verify bay size/interface compatibility); after replacement use UGOS Storage app "Repair" to rebuild mirror onto new drive; locate correct UGOS menu for per-share recycle bin settings on proxmox-backups and disable it (not found in Storage app sections checked this session); add CT 306 and CT 307 to backup-daily VMID list via Proxmox Datacenter → Backup → Edit; consider updating Prometheus CPU alert rule to exclude iowait from calculation; monitor Hard Drive 2 closely until Hard Drive 1 is replaced (interim zero-redundancy state)

### 2026-07-17 — Maintenance Session — Palworld Server Connectivity & Configuration Fixes
- External friend unable to connect to Palworld server (CT 307); diagnosed PublicIP misconfiguration in PalWorldSettings.ini pointing to container's internal LAN IP (192.168.30.219) instead of public WAN IP
- Fixed PublicIP field by enabling User Editable + User Viewable permissions on Pelican egg "Public IP" variable (previously both unchecked, blocking manual updates via client Startup tab)
- Discovered Pelican's PalworldServerConfigParser auto-populates PublicIP from local allocation whenever the egg variable is empty, causing silent reverts of manual ini edits
- Attempted PalCaptureRate edit via Pelican web file editor; discovered editor introduces hard line breaks into PalWorldSettings.ini's OptionSettings=(…) block, causing Palworld to silently ignore entire settings block and fall back to defaults (confirmed PalCaptureRate=100 change took effect only after collapsing corrupted 13-line file back to 2 lines)
- Standardized PalWorldSettings.ini edits to `pct exec` + `sed` commands from Proxmox host; abandoned Pelican web editor due to confirmed line-break corruption risk
- Pelican panel file Download function returns generic 404 "resource not found" error for Palworld egg/node even on internal IP access; root cause suspected to be Wings/node FQDN misconfiguration in signed URL generation (not investigated further; bypassed with pct exec + pct pull backup method)
- Established reliable backup method for Pelican-managed container data: `pct exec <CTID> -- tar` followed by `pct pull` from Proxmox host, bypassing Wings panel entirely; created SaveGames backup (22M) at /mnt/hdd-backup-1/palworld-backup-20260716/
- Confirmed current Palworld version does not lock settings into world save file (no WorldOption.sav exists); PalWorldSettings.ini remains authoritative for gameplay settings on every boot
- Diagnosed in-game stutter with 3 concurrent players; root cause: scheduled nightly vzdump backup job (sequential, all-container) running concurrently with active gameplay hours, competing for host CPU/disk I/O (CT 222 ntfy, CT 223 Langfuse observed backing up during session starting ~02:11 AM)
- NOT a Palworld resource allocation or hardware capacity issue; decided to consolidate Palworld/Terraria restart schedules with nightly vzdump backup window into single off-peak maintenance block (exact time window pending confirmation of reliably dead low-usage hour)
- WAN IP instability confirmed: IP changed 3 times in ~1 week due to TIME PPPoE session renegotiation (202.184.101.136 → 202.184.103.49 → 202.184.109.124); second connectivity drop occurred mid-session (202.184.103.49 → 202.184.109.124), fixed with manual PublicIP update process
- PublicIP updated to 202.184.109.124 as of session end (will drift again on next PPPoE renegotiation — known unresolved issue)
- PalCaptureRate reverted to default (1.000000) after testing at 100.000000; backup of corrupted ini saved as PalWorldSettings.ini.bak for reference
- Decision: Elevated DDNS automation priority (previously queued roadmap item) ahead of backup/restart consolidation work, given three WAN IP changes in approximately one week
- Action items: Update Cloudflare DNS records for other game server subdomains (Terraria, Minecraft, Enshrouded on najhin-gaming.com) currently pointing at stale IPs; verify CT 207 ddclient scope and extend to cover Palworld PublicIP field automation if needed; investigate Pelican panel Download function root cause (check Admin → Nodes for FQDN/signed URL mismatch); determine reliable off-peak maintenance window and reschedule vzdump backup job via Datacenter → Backup; set up Pelican per-server Schedules for Palworld/Terraria restarts; investigate long-running dotnet process (PID 16195, 6+ days at ~99% CPU on one thread) observed during stutter diagnosis

### 2026-07-14 — Monitoring Bug Fix — node_exporter /mnt Exclusion
- Investigated 8-day-old "hdd-backup-1 not mounted" critical Discord alert (fingerprint fc914336cd6ed15b, active since 2026-07-06T10:54:56Z)
- Verified drive was actually mounted and healthy at OS level (5.5T total, 18% used, device /dev/sdb1, ext4, UUID 5593849e-8ee9-4d6c-b1bc-3e35650e05fb); SATA link errors from 2026-07-06 dmesg were one-time self-resolved incident, not ongoing fault
- Root cause: Debian package `prometheus-node-exporter.service` ships with default `--collector.filesystem.mount-points-exclude` regex containing `mnt`, making ALL `/mnt/*` mountpoints invisible to Prometheus since at least 2026-05-16
- Fixed by editing `/etc/default/prometheus-node-exporter` to remove `mnt` from exclude regex: changed `^/(dev|proc|run|sys|mnt|media|var/lib/docker/.+|var/lib/containers/storage/.+)($|/)` to `^/(dev|proc|run|sys|media|var/lib/docker/.+|var/lib/containers/storage/.+)($|/)`
- Restarted `prometheus-node-exporter.service` (PID 1307682 post-fix), confirmed all three /mnt mountpoints (hdd-backup-1, hdd-backup-2, pve/kinmoon-smb) now report metrics to Prometheus, alert auto-resolved
- Clarified storage architecture: hdd-backup-1 is primary live storage (Nextcloud data directory bind mount), not a backup copy; Kinmoon NAS receives nightly rsync copy at 03:00 (one-way, time-lagged, not live mirror)
- Confirmed hdd-backup-2 also healthy (6.9T total, 0% used, device /dev/sdc1, ext4, UUID 5cd7f4a4-7510-42eb-9f35-be49f6a10686)
- Physical SATA cable/port swap remains deferred, no budget currently available
- Decision: Root-caused and fixed node_exporter config bug rather than silencing Alertmanager rule — alert was legitimate in intent, detection mechanism was broken
- Action items: Verify Proxmox vzdump backup jobs targeting correct storage (local hdd-backup-* vs /mnt/pve/kinmoon-smb SMB); check Grafana dashboards for /mnt metric panels silent since ~May 16, 2026 now that fix is live; confirm backup jobs landing in expected location

### 2026-07-09 — Jeanne Alter Email Management Pipeline — Design Complete
- Audited personal email accounts (~15 discovered via Google account list and iPhone